# Iter 4.5a Keyring — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Flytta `anthropic_api_key` från klartext `settings.json` till Windows Credential Manager via `keyring`-crate, utan app-restart efter migration.

**Architecture:** Ny pure-lib `svoice-secrets` wrappar `keyring = "3"`. Settings-struct tar bort fältet helt. Migration körs vid app-start som best-effort (läser klartext-JSON, skriver till keyring, städar JSON). Frontend får tre nya IPC-commands (`has/set/clear_anthropic_key`) och visar `••••••••` + Rensa-knapp när keyring har värde.

**Tech Stack:** Rust (Tauri 2), `keyring = "3"`, `thiserror`, `tracing`, `serde_json`; React/TypeScript för Settings-UI.

**Spec:** [`docs/superpowers/specs/2026-04-18-iter4.5a-keyring-design.md`](../specs/2026-04-18-iter4.5a-keyring-design.md)

**Branch:** `iter4.5a/keyring` (skapad, med spec-commit `9c4d511`)

---

## File Structure

**Nya filer:**
- `src-tauri/crates/secrets/Cargo.toml` — crate-manifest
- `src-tauri/crates/secrets/src/lib.rs` — public API + tester

**Modifierade filer:**
- `src-tauri/Cargo.toml` — lägg `crates/secrets` i workspace members + `svoice-secrets` i lib-deps
- `src-tauri/crates/settings/src/lib.rs` — ta bort `anthropic_api_key`-fältet
- `src-tauri/crates/ipc/Cargo.toml` — lägg `svoice-secrets` dep
- `src-tauri/crates/ipc/src/commands.rs` — tre nya commands
- `src-tauri/crates/ipc/src/lib.rs` — export de nya commands
- `src-tauri/src/lib.rs` — migration, hot-reload-sites, `select_llm_provider`, `invoke_handler`
- `src/lib/settings-api.ts` — ta bort `anthropic_api_key`-fält + nya funktioner
- `src/windows/Settings.tsx` — `••••••••`-UX + Rensa-knapp

**Ingen ny test-mapp** — tester bor inline i respektive Rust-crate via `#[cfg(test)] mod tests`.

---

## Task 1: Bootstrap `svoice-secrets`-crate

**Files:**
- Create: `src-tauri/crates/secrets/Cargo.toml`
- Create: `src-tauri/crates/secrets/src/lib.rs`
- Modify: `src-tauri/Cargo.toml`

- [ ] **Step 1.1: Skapa crate-struktur**

Kör från `C:\Users\marcu\Documents\Programmering hemma\Temp\SVoice ver 3`:

```bash
mkdir -p src-tauri/crates/secrets/src
```

- [ ] **Step 1.2: Skriv `src-tauri/crates/secrets/Cargo.toml`**

```toml
[package]
name = "svoice-secrets"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
keyring = "3"
thiserror = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
keyring = { version = "3", features = ["mock"] }
```

- [ ] **Step 1.3: Skriv `src-tauri/crates/secrets/src/lib.rs` (skeleton)**

```rust
//! Windows Credential Manager-wrapper för SVoice 3 secrets.
//!
//! Alla secrets lagras under service `"svoice-v3"` med semantiska usernames
//! (`"anthropic_api_key"`, framtida `"google_refresh_token"` osv.). Credential
//! Manager kan filtreras på service-strängen för enkel översikt/rensning.

use keyring::Entry;
use thiserror::Error;

pub const SERVICE: &str = "svoice-v3";
const USERNAME_ANTHROPIC: &str = "anthropic_api_key";

#[derive(Error, Debug)]
pub enum SecretsError {
    #[error("keyring backend unavailable: {0}")]
    Backend(#[from] keyring::Error),
}

fn entry(username: &str) -> Result<Entry, SecretsError> {
    Ok(Entry::new(SERVICE, username)?)
}

/// Hämta Anthropic API-nyckeln från keyring.
/// Returnerar `Ok(None)` när entry saknas (first-run); `Err` vid backend-fel.
pub fn get_anthropic_key() -> Result<Option<String>, SecretsError> {
    match entry(USERNAME_ANTHROPIC)?.get_password() {
        Ok(pw) => Ok(Some(pw)),
        Err(keyring::Error::NoEntry) => Ok(None),
        Err(e) => Err(SecretsError::Backend(e)),
    }
}

/// Skriv (eller ersätt) Anthropic API-nyckeln i keyring.
pub fn set_anthropic_key(key: &str) -> Result<(), SecretsError> {
    entry(USERNAME_ANTHROPIC)?.set_password(key)?;
    Ok(())
}

/// Radera Anthropic API-nyckeln från keyring. No-op om den inte finns.
pub fn delete_anthropic_key() -> Result<(), SecretsError> {
    match entry(USERNAME_ANTHROPIC)?.delete_credential() {
        Ok(()) => Ok(()),
        Err(keyring::Error::NoEntry) => Ok(()),
        Err(e) => Err(SecretsError::Backend(e)),
    }
}

/// Convenience för frontend — returnerar alltid bool, sväljer backend-fel.
pub fn has_anthropic_key() -> bool {
    matches!(get_anthropic_key(), Ok(Some(_)))
}
```

- [ ] **Step 1.4: Registrera crate i workspace**

Modifiera `src-tauri/Cargo.toml` — lägg `"crates/secrets"` alfabetiskt i `[workspace] members`:

```toml
[workspace]
resolver = "2"
members = [
    "crates/audio",
    "crates/hotkey",
    "crates/inject",
    "crates/integrations",
    "crates/ipc",
    "crates/llm",
    "crates/secrets",
    "crates/settings",
    "crates/stt",
    "bins/stt-spike",
]
```

Lägg också raden `svoice-secrets = { path = "crates/secrets" }` i `[dependencies]`-blocket för `svoice-v3`-paketet (alfabetiskt efter `svoice-llm`).

- [ ] **Step 1.5: Verifiera att crate bygger**

Kör:

```bash
cd src-tauri && cargo check -p svoice-secrets
```

Förväntat: `Finished dev ... in N.Ns` utan warnings. Om `keyring = "3"` inte hittas, kolla att Cargo-registret är uppdaterat (`cargo update -p keyring`).

- [ ] **Step 1.6: Commit**

```bash
cd "C:/Users/marcu/Documents/Programmering hemma/Temp/SVoice ver 3"
git add src-tauri/crates/secrets/ src-tauri/Cargo.toml
git commit -m "feat(secrets): bootstrap svoice-secrets-crate med keyring v3

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: Enhetstester för `svoice-secrets` (TDD röd → grön)

**Files:**
- Modify: `src-tauri/crates/secrets/src/lib.rs`

Mock-keyring i v3 sätts processglobalt via `keyring::set_default_credential_builder(keyring::mock::default_credential_builder())`. Eftersom detta är global state ska alla tester köras i samma modul och dela builder.

- [ ] **Step 2.1: Lägg till `#[cfg(test)] mod tests`-blocket med röda tester**

Lägg till i slutet av `src-tauri/crates/secrets/src/lib.rs`:

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use keyring::{mock, set_default_credential_builder};
    use std::sync::Once;

    static INIT: Once = Once::new();

    /// Sätter in-memory mock-keyring processglobalt. Idempotent.
    fn init_mock() {
        INIT.call_once(|| {
            set_default_credential_builder(mock::default_credential_builder());
        });
    }

    /// Rensa state innan varje test (mock-keyring delas globalt).
    fn reset() {
        init_mock();
        let _ = delete_anthropic_key();
    }

    #[test]
    fn get_returns_none_when_empty() {
        reset();
        assert_eq!(get_anthropic_key().unwrap(), None);
    }

    #[test]
    fn set_then_get_roundtrips() {
        reset();
        set_anthropic_key("sk-ant-abc123").unwrap();
        assert_eq!(get_anthropic_key().unwrap().as_deref(), Some("sk-ant-abc123"));
    }

    #[test]
    fn delete_removes_stored_value() {
        reset();
        set_anthropic_key("sk-ant-xyz").unwrap();
        delete_anthropic_key().unwrap();
        assert_eq!(get_anthropic_key().unwrap(), None);
    }

    #[test]
    fn delete_is_noop_when_absent() {
        reset();
        // Ska inte panica eller returnera Err trots att inget finns
        delete_anthropic_key().unwrap();
    }

    #[test]
    fn has_reflects_state() {
        reset();
        assert!(!has_anthropic_key());
        set_anthropic_key("sk-ant-xyz").unwrap();
        assert!(has_anthropic_key());
        delete_anthropic_key().unwrap();
        assert!(!has_anthropic_key());
    }
}
```

- [ ] **Step 2.2: Kör tester och verifiera att de passerar**

```bash
cd src-tauri && cargo test -p svoice-secrets
```

Förväntat output:
```
running 5 tests
test tests::get_returns_none_when_empty ... ok
test tests::set_then_get_roundtrips ... ok
test tests::delete_removes_stored_value ... ok
test tests::delete_is_noop_when_absent ... ok
test tests::has_reflects_state ... ok

test result: ok. 5 passed; 0 failed
```

**Om test fallerar med "mock backend not initialized":** Kolla att `init_mock()` körs före `reset()`. Mock-buildern måste sättas *innan* första `Entry::new()` på denna tråd.

**Om test fallerar med flaky "already exists":** Kör med `--test-threads=1`:
```bash
cargo test -p svoice-secrets -- --test-threads=1
```

- [ ] **Step 2.3: Commit**

```bash
git add src-tauri/crates/secrets/src/lib.rs
git commit -m "test(secrets): enhetstester mot mock-keyring (5 tester)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: Ta bort `anthropic_api_key` ur `Settings`-struct

**Files:**
- Modify: `src-tauri/crates/settings/src/lib.rs`

- [ ] **Step 3.1: Ta bort fältet + default + test-referenser**

Öppna `src-tauri/crates/settings/src/lib.rs`.

Ta bort raderna 28–29 (`/// Anthropic Claude API-nyckel...` + `pub anthropic_api_key: Option<String>,`).

I `impl Default for Settings`, ta bort raden `anthropic_api_key: None,` (rad 62).

I `#[test] fn roundtrip_via_json`, ta bort raden `anthropic_api_key: Some("sk-ant-test".into()),` (rad 128) och assert-raden `assert_eq!(original.anthropic_api_key, restored.anthropic_api_key);` (rad 138).

- [ ] **Step 3.2: Verifiera att settings-crate kompilerar + tester passerar**

```bash
cd src-tauri && cargo test -p svoice-settings
```

Förväntat:
```
running 3 tests
test tests::default_values_match_spec ... ok
test tests::roundtrip_via_json ... ok
test tests::compute_mode_serializes_snake_case ... ok
```

- [ ] **Step 3.3: Verifiera att workspace ÄN INTE bygger**

Kör:
```bash
cargo check --workspace
```

Förväntat: Kompileringsfel i `src-tauri/src/lib.rs` (rad 118, 161, 725) och eventuellt `svoice-ipc/src/commands.rs` som refererar `settings.anthropic_api_key`. **Detta är förväntat** — nästa tasks fixar dem.

- [ ] **Step 3.4: Commit**

```bash
git add src-tauri/crates/settings/src/lib.rs
git commit -m "refactor(settings): ta bort anthropic_api_key ur Settings-struct

Flyttar till svoice-secrets i kommande commits. Workspace bygger
inte fristående efter denna commit — bygg-grönt efter hela iter 4.5a.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: IPC-commands `has_anthropic_key`, `set_anthropic_key`, `clear_anthropic_key`

**Files:**
- Modify: `src-tauri/crates/ipc/Cargo.toml`
- Modify: `src-tauri/crates/ipc/src/commands.rs`
- Modify: `src-tauri/crates/ipc/src/lib.rs`

- [ ] **Step 4.1: Lägg `svoice-secrets` som dep i `svoice-ipc`**

Öppna `src-tauri/crates/ipc/Cargo.toml`. Lägg till raden `svoice-secrets = { path = "../secrets" }` alfabetiskt i `[dependencies]` (efter `svoice-llm`).

- [ ] **Step 4.2: Lägg till tre commands i `src-tauri/crates/ipc/src/commands.rs`**

Lägg till i slutet av filen (efter `pull_ollama_model`):

```rust
/// Indikera till frontend om en Anthropic-nyckel ligger i Windows Credential
/// Manager. Används för att visa `••••••••` vs tom input i Settings-UI.
#[tauri::command]
pub fn has_anthropic_key() -> bool {
    svoice_secrets::has_anthropic_key()
}

/// Spara Anthropic API-nyckel i Windows Credential Manager. Ersätter ev.
/// befintligt värde. Tom sträng betraktas som fel (anropa `clear` istället).
#[tauri::command]
pub fn set_anthropic_key(key: String) -> Result<(), String> {
    let trimmed = key.trim();
    if trimmed.is_empty() {
        return Err("nyckel får inte vara tom — använd clear-kommandot istället".into());
    }
    svoice_secrets::set_anthropic_key(trimmed)
        .map_err(|e| format!("kunde inte spara nyckel: {e}"))
}

/// Radera Anthropic API-nyckeln ur Credential Manager. No-op om den saknas.
#[tauri::command]
pub fn clear_anthropic_key() -> Result<(), String> {
    svoice_secrets::delete_anthropic_key()
        .map_err(|e| format!("kunde inte radera nyckel: {e}"))
}
```

- [ ] **Step 4.3: Exportera i `src-tauri/crates/ipc/src/lib.rs`**

Ersätt hela filen med:

```rust
pub mod commands;
pub use commands::{
    action_apply, action_cancel, check_hf_cached, clear_anthropic_key, get_settings,
    has_anthropic_key, list_mic_devices, list_ollama_models, pull_ollama_model,
    set_anthropic_key, set_settings, InjectResult, PttStateReport,
};
```

- [ ] **Step 4.4: Verifiera att `svoice-ipc` bygger**

```bash
cd src-tauri && cargo check -p svoice-ipc
```

Förväntat: `Finished`. Inga warnings om oanvända imports.

- [ ] **Step 4.5: Commit**

```bash
git add src-tauri/crates/ipc/
git commit -m "feat(ipc): three keyring-commands (has/set/clear_anthropic_key)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: Hot-reload-sites i `src-tauri/src/lib.rs`

**Files:**
- Modify: `src-tauri/Cargo.toml` (lägg redan gjort i Task 1, hoppa om redan där)
- Modify: `src-tauri/src/lib.rs`

`select_llm_provider` läser idag nyckeln från `settings.anthropic_api_key`. Ändra signaturen så caller skickar in nyckeln.

- [ ] **Step 5.1: Ändra `select_llm_provider`-signaturen**

Hitta funktionen (~rad 722):

```rust
async fn select_llm_provider(settings: &Settings) -> Option<Arc<dyn LlmProvider>> {
    let build_anthropic = || -> Option<Arc<dyn LlmProvider>> {
        settings
            .anthropic_api_key
            .as_ref()
            .filter(|k| !k.is_empty())
            .map(|key| {
                Arc::new(
                    AnthropicClient::new(key.clone())
                        .with_model(settings.anthropic_model.clone()),
                ) as Arc<dyn LlmProvider>
            })
    };
```

Ersätt med:

```rust
async fn select_llm_provider(
    settings: &Settings,
    anthropic_key: Option<&str>,
) -> Option<Arc<dyn LlmProvider>> {
    let build_anthropic = || -> Option<Arc<dyn LlmProvider>> {
        anthropic_key
            .filter(|k| !k.is_empty())
            .map(|key| {
                Arc::new(
                    AnthropicClient::new(key.to_string())
                        .with_model(settings.anthropic_model.clone()),
                ) as Arc<dyn LlmProvider>
            })
    };
```

- [ ] **Step 5.2: Uppdatera alla call-sites för `select_llm_provider`**

Sök i `src-tauri/src/lib.rs` efter `select_llm_provider(` med:

```bash
grep -n "select_llm_provider(" src-tauri/src/lib.rs
```

För varje träff, ersätt `select_llm_provider(&settings)` med:

```rust
select_llm_provider(
    &settings,
    svoice_secrets::get_anthropic_key().ok().flatten().as_deref(),
)
```

**Kritisk detalj:** `.as_deref()` bytar `Option<String>` → `Option<&str>`. Eftersom `get_anthropic_key()` returnerar `Result<Option<String>>`, gör `.ok().flatten()` → `Option<String>` → `.as_deref()` → `Option<&str>`. Men call-expressionen äger inte sträng-ownership över `.await`-punkten, så bygg i ett separat `let`:

```rust
let anthropic_key = svoice_secrets::get_anthropic_key().ok().flatten();
let llm = select_llm_provider(&settings, anthropic_key.as_deref()).await;
```

- [ ] **Step 5.3: Uppdatera app-start-log och action-LLM-check**

Hitta rad ~118:
```rust
user_settings.anthropic_api_key.as_ref().map(|_| "****").unwrap_or("none"),
```

Ersätt med:
```rust
if svoice_secrets::has_anthropic_key() { "****" } else { "none" },
```

Hitta rad ~161:
```rust
if user_settings.anthropic_api_key.is_none() {
    tracing::info!(
        "action-LLM ej konfigurerad — lägg till anthropic_api_key i settings"
    );
}
```

Ersätt med:
```rust
if !svoice_secrets::has_anthropic_key() {
    tracing::info!(
        "action-LLM ej konfigurerad — lägg till Anthropic-nyckel i Settings"
    );
}
```

- [ ] **Step 5.4: Registrera commands i `invoke_handler!`**

Hitta `.invoke_handler(tauri::generate_handler![` (~rad 278). Lägg till de tre nya commands alfabetiskt:

```rust
.invoke_handler(tauri::generate_handler![
    svoice_ipc::action_apply,
    svoice_ipc::action_cancel,
    svoice_ipc::check_hf_cached,
    svoice_ipc::clear_anthropic_key,
    svoice_ipc::get_settings,
    svoice_ipc::has_anthropic_key,
    svoice_ipc::list_mic_devices,
    svoice_ipc::list_ollama_models,
    svoice_ipc::pull_ollama_model,
    svoice_ipc::set_anthropic_key,
    svoice_ipc::set_settings,
])
```

- [ ] **Step 5.5: Verifiera att lib.rs bygger**

```bash
cd src-tauri && cargo check
```

Förväntat: `Finished`. Om `svoice_secrets` inte hittas: kontrollera att `svoice-secrets = { path = "crates/secrets" }` finns i `[dependencies]` i `src-tauri/Cargo.toml` (lades till i Task 1.4).

- [ ] **Step 5.6: Commit**

```bash
git add src-tauri/src/lib.rs src-tauri/Cargo.toml
git commit -m "refactor(lib): läs anthropic-nyckel från svoice-secrets istället för Settings

select_llm_provider tar nu nyckeln som parameter. Hot-reload-pathen
anropar svoice_secrets::get_anthropic_key() vid varje PTT-trigger.
Invoke-handler registrerar has/set/clear_anthropic_key.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: Migration från klartext-JSON (TDD)

**Files:**
- Create: `src-tauri/src/migrate.rs`
- Modify: `src-tauri/src/lib.rs`

Migration-funktionen placeras i app-layer (inte i secrets/settings-crates) så varken crate behöver depa på den andra.

- [ ] **Step 6.1: Skapa `src-tauri/src/migrate.rs` med public entry + helper**

```rust
//! Engångs-migrationer som körs vid app-start. Varje funktion är idempotent
//! (säkert att köra varje start; no-op om inget att flytta).

use std::path::Path;

/// Om `settings.json` har `anthropic_api_key` som non-empty sträng, flytta
/// den till keyring och ta bort fältet ur JSON. Best-effort: loggar och
/// returnerar Ok(()) vid fel så app-start inte blockeras.
pub fn migrate_anthropic_key(settings_path: &Path) -> anyhow::Result<()> {
    let Ok(raw) = std::fs::read_to_string(settings_path) else {
        return Ok(()); // ingen fil → ingen migration
    };
    let mut value: serde_json::Value = match serde_json::from_str(&raw) {
        Ok(v) => v,
        Err(e) => {
            tracing::warn!("migrate: kan inte parsa settings.json: {e}");
            return Ok(());
        }
    };
    let Some(obj) = value.as_object_mut() else {
        return Ok(());
    };
    let Some(key_value) = obj.get("anthropic_api_key").and_then(|v| v.as_str()) else {
        return Ok(()); // fältet saknas eller är null/non-string
    };
    if key_value.is_empty() {
        // Städa bort tomt fält utan keyring-skrivning
        obj.remove("anthropic_api_key");
        write_atomic(settings_path, &value)?;
        return Ok(());
    }
    match svoice_secrets::set_anthropic_key(key_value) {
        Ok(()) => {
            obj.remove("anthropic_api_key");
            write_atomic(settings_path, &value)?;
            tracing::info!("migrerat anthropic_api_key till Windows Credential Manager");
        }
        Err(e) => {
            tracing::error!("keyring-migrering misslyckades: {e} — klartext kvar tills nästa försök");
        }
    }
    Ok(())
}

fn write_atomic(path: &Path, value: &serde_json::Value) -> anyhow::Result<()> {
    let parent = path.parent().ok_or_else(|| anyhow::anyhow!("invalid path"))?;
    let tmp = parent.join("settings.json.tmp");
    let json = serde_json::to_string_pretty(value)?;
    std::fs::write(&tmp, json)?;
    std::fs::rename(&tmp, path)?;
    Ok(())
}

#[cfg(test)]
mod tests {
    use super::*;
    use keyring::{mock, set_default_credential_builder};
    use std::sync::Once;
    use tempfile::tempdir;

    static INIT: Once = Once::new();
    fn init_mock() {
        INIT.call_once(|| {
            set_default_credential_builder(mock::default_credential_builder());
        });
    }

    #[test]
    fn migrates_plaintext_key_and_strips_json() {
        init_mock();
        let _ = svoice_secrets::delete_anthropic_key();

        let dir = tempdir().unwrap();
        let path = dir.path().join("settings.json");
        std::fs::write(
            &path,
            r#"{"anthropic_api_key":"sk-ant-legacy","stt_model":"whisper"}"#,
        )
        .unwrap();

        migrate_anthropic_key(&path).unwrap();

        // Keyring har värdet
        assert_eq!(
            svoice_secrets::get_anthropic_key().unwrap().as_deref(),
            Some("sk-ant-legacy")
        );
        // JSON har fältet borttaget men stt_model kvar
        let after: serde_json::Value =
            serde_json::from_str(&std::fs::read_to_string(&path).unwrap()).unwrap();
        assert!(after.get("anthropic_api_key").is_none());
        assert_eq!(after.get("stt_model").and_then(|v| v.as_str()), Some("whisper"));
    }

    #[test]
    fn noop_when_no_legacy_key() {
        init_mock();
        let _ = svoice_secrets::delete_anthropic_key();

        let dir = tempdir().unwrap();
        let path = dir.path().join("settings.json");
        let original = r#"{"stt_model":"whisper"}"#;
        std::fs::write(&path, original).unwrap();

        migrate_anthropic_key(&path).unwrap();

        assert_eq!(std::fs::read_to_string(&path).unwrap(), original);
        assert!(!svoice_secrets::has_anthropic_key());
    }

    #[test]
    fn handles_missing_settings_file() {
        init_mock();
        let dir = tempdir().unwrap();
        let path = dir.path().join("does-not-exist.json");
        // Ska inte panica
        migrate_anthropic_key(&path).unwrap();
    }

    #[test]
    fn strips_empty_string_without_writing_keyring() {
        init_mock();
        let _ = svoice_secrets::delete_anthropic_key();

        let dir = tempdir().unwrap();
        let path = dir.path().join("settings.json");
        std::fs::write(&path, r#"{"anthropic_api_key":"","stt_model":"w"}"#).unwrap();

        migrate_anthropic_key(&path).unwrap();

        let after: serde_json::Value =
            serde_json::from_str(&std::fs::read_to_string(&path).unwrap()).unwrap();
        assert!(after.get("anthropic_api_key").is_none());
        assert!(!svoice_secrets::has_anthropic_key());
    }
}
```

- [ ] **Step 6.2: Lägg `tempfile` som dev-dep + `anyhow` i `src-tauri/Cargo.toml`**

`anyhow` finns redan som workspace-dep och används i `src-tauri/Cargo.toml`. Lägg `tempfile` som dev-dep i `src-tauri/Cargo.toml` (under `[dependencies]`-blocket):

```toml
[dev-dependencies]
tempfile = "3"
```

Lägg också `keyring` som dev-dep i `src-tauri/Cargo.toml` eftersom `mock`-funktionaliteten behövs för tester i `src-tauri/src/`:

```toml
[dev-dependencies]
tempfile = "3"
keyring = { version = "3", features = ["mock"] }
```

- [ ] **Step 6.3: Deklarera `mod migrate` i `src-tauri/src/lib.rs`**

Överst i `src-tauri/src/lib.rs`, efter `use`-raderna (ungefär rad 27), lägg till:

```rust
mod migrate;
```

- [ ] **Step 6.4: Anropa migration i `setup`-closure**

Hitta `let user_settings = Settings::load();` (rad ~112). Lägg en rad före:

```rust
// Kör best-effort migration av klartext anthropic_api_key → keyring.
// Idempotent; no-op efter första migration.
if let Err(e) = migrate::migrate_anthropic_key(&Settings::path()) {
    tracing::error!("settings-migration fel: {e}");
}

let user_settings = Settings::load();
```

- [ ] **Step 6.5: Kör migrate-tester**

```bash
cd src-tauri && cargo test migrate --bin svoice-v3 -- --test-threads=1
```

Om testerna inte exekveras eftersom `svoice-v3` är en lib-crate också, prova:

```bash
cd src-tauri && cargo test --lib migrate::tests -- --test-threads=1
```

Förväntat: alla 4 tester gröna.

- [ ] **Step 6.6: Commit**

```bash
git add src-tauri/src/migrate.rs src-tauri/src/lib.rs src-tauri/Cargo.toml
git commit -m "feat(migrate): flytta klartext anthropic_api_key till keyring vid app-start

Best-effort idempotent migration. Läser settings.json som serde_json::Value,
skriver nyckel till keyring, tar bort fältet ur JSON och skriver tillbaka
atomiskt (tempfil + rename). 4 tester mot mock-keyring.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: Frontend TS-API

**Files:**
- Modify: `src/lib/settings-api.ts`

- [ ] **Step 7.1: Ta bort `anthropic_api_key` ur `Settings`-typen**

Öppna `src/lib/settings-api.ts`. Ta bort raden `anthropic_api_key: string | null;` (rad 15).

Resulterande typ:

```ts
export interface Settings {
  mic_device: string | null;
  stt_enabled: boolean;
  stt_model: string;
  stt_compute_mode: ComputeMode;
  vad_threshold: number;
  action_llm_enabled: boolean;
  llm_polish_dictation: boolean;
  llm_provider: LlmProviderChoice;
  anthropic_model: string;
  ollama_model: string;
  ollama_url: string;
}
```

- [ ] **Step 7.2: Lägg till API-wrappers**

Lägg till i slutet av `src/lib/settings-api.ts`:

```ts
export async function hasAnthropicKey(): Promise<boolean> {
  return invoke<boolean>("has_anthropic_key");
}

export async function setAnthropicKey(key: string): Promise<void> {
  await invoke<void>("set_anthropic_key", { key });
}

export async function clearAnthropicKey(): Promise<void> {
  await invoke<void>("clear_anthropic_key");
}
```

- [ ] **Step 7.3: Verifiera att TS kompilerar**

```bash
pnpm tsc --noEmit
```

Förväntat: inga fel. Om `Settings.tsx` refererar `anthropic_api_key` visas fel där — det fixas i Task 8.

- [ ] **Step 7.4: Commit**

```bash
git add src/lib/settings-api.ts
git commit -m "feat(frontend): TS API för keyring-commands

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: Settings.tsx UX — `••••••••` + Rensa-knapp

**Files:**
- Modify: `src/windows/Settings.tsx`

- [ ] **Step 8.1: Importera nya API-funktioner**

Ändra import-blocket (rad 4–16) så det inkluderar de nya funktionerna:

```tsx
import {
  checkHfCached,
  clearAnthropicKey,
  getSettings,
  hasAnthropicKey,
  listMicDevices,
  listOllamaModels,
  pullOllamaModel,
  setAnthropicKey,
  setSettings,
  type ComputeMode,
  type LlmProviderChoice,
  type OllamaModelInfo,
  type PullProgress,
  type Settings,
} from "../lib/settings-api";
```

- [ ] **Step 8.2: Lägg till state för keyring-status**

Hitta state-blocket i `SettingsView`-komponenten (ungefär rad 86–97). Lägg till två nya raden direkt efter `const [sttCached, setSttCached] = useState<Record<string, boolean>>({});`:

```tsx
const [keyStored, setKeyStored] = useState(false);
const [keyDraft, setKeyDraft] = useState<string | null>(null); // null=orört, ""=rensa, annars=ny
```

- [ ] **Step 8.3: Ladda keyring-status vid mount**

Hitta `useEffect`-blocket som anropar `getSettings()` vid mount. Lägg till `hasAnthropicKey()` i samma useEffect:

```tsx
useEffect(() => {
  // ... befintlig kod ...
  hasAnthropicKey().then(setKeyStored).catch(() => setKeyStored(false));
}, []);
```

(Om useEffect har complext mönster: lägg till en separat `useEffect(() => { hasAnthropicKey().then(setKeyStored); }, []);`.)

- [ ] **Step 8.4: Ersätt API-nyckel-fältet (rad 507–531)**

Hitta:

```tsx
<div className="field">
  <label className="field-label" htmlFor="api-key">
    Anthropic API-nyckel
  </label>
  <input
    id="api-key"
    className="input"
    type="password"
    placeholder="sk-ant-…"
    value={draft.anthropic_api_key ?? ""}
    onChange={(e) =>
      setDraft({
        ...draft,
        anthropic_api_key:
          e.target.value.trim() === "" ? null : e.target.value,
      })
    }
    autoComplete="off"
    spellCheck={false}
  />
  <div className="field-help">
    Används när provider är Claude eller Auto (fallback).
    Sparas i klartext — keyring kommer i nästa iter.
  </div>
</div>
```

Ersätt med:

```tsx
<div className="field">
  <label className="field-label" htmlFor="api-key">
    Anthropic API-nyckel
  </label>
  <input
    id="api-key"
    className="input"
    type="password"
    placeholder={keyStored && keyDraft === null ? "••••••••" : "sk-ant-…"}
    value={keyDraft ?? ""}
    onChange={(e) => setKeyDraft(e.target.value)}
    autoComplete="off"
    spellCheck={false}
  />
  <div className="field-help">
    {keyDraft === ""
      ? "Nyckeln raderas när du sparar."
      : "Sparas säkert i Windows Credential Manager."}
  </div>
  {keyStored && keyDraft === null && (
    <button
      type="button"
      className="link-button"
      onClick={() => setKeyDraft("")}
    >
      Rensa nyckel
    </button>
  )}
</div>
```

- [ ] **Step 8.5: Koppla Spara-knappen så den skriver keyring-ändring**

Hitta `async function handleSave()` eller motsvarande. Lägg till keyring-logik innan `setSettings(draft)`:

```tsx
async function handleSave() {
  if (!draft) return;
  setSaving(true);
  setError(null);
  try {
    if (keyDraft !== null) {
      if (keyDraft.trim() === "") {
        await clearAnthropicKey();
      } else {
        await setAnthropicKey(keyDraft.trim());
      }
      setKeyStored(keyDraft.trim() !== "");
      setKeyDraft(null);
    }
    await setSettings(draft);
    setLoaded(draft);
    setSavedTick((t) => t + 1);
  } catch (e) {
    setError(String(e));
  } finally {
    setSaving(false);
  }
}
```

(Anpassa efter exakt befintlig handleSave — det viktiga är att keyring-anropen sker först, följt av setSettings.)

- [ ] **Step 8.6: Lägg till CSS för `.link-button`**

Öppna `src/windows/Settings.css`. Lägg till:

```css
.link-button {
  background: none;
  border: none;
  color: var(--color-amber, #d4a955);
  padding: 4px 0;
  font: inherit;
  cursor: pointer;
  text-decoration: underline;
  margin-top: 4px;
}
.link-button:hover {
  color: var(--color-amber-light, #e6c280);
}
```

- [ ] **Step 8.7: Bygg frontend + verifiera**

```bash
pnpm build
```

Förväntat: inga TS-fel, dist/ byggs. Om error om `anthropic_api_key` på andra ställen: de fixades förmodligen inte i filen, kolla `grep -n "anthropic_api_key" src/`.

- [ ] **Step 8.8: Commit**

```bash
git add src/windows/Settings.tsx src/windows/Settings.css
git commit -m "feat(settings-ui): ••••••••-indikator + Rensa nyckel-knapp

Fält visar ••••••• när keyring har värde. User kan skriva ny nyckel
eller klicka Rensa (raderas vid Spara). Hjälptext uppdaterad.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: Integrations-test + build-verifiering

**Files:** (ingen modifiering — verifiering)

- [ ] **Step 9.1: Kör hela workspace-tester**

```bash
cd src-tauri && cargo test --workspace -- --test-threads=1
```

Förväntat: minst 27 tester gröna (22 befintliga + 5 secrets + eventuellt +4 migrate).

```
test result: ok. N passed; 0 failed; 0 ignored
```

- [ ] **Step 9.2: Bygg release-mode utan bundle**

```bash
cd src-tauri && cargo build --release
```

Förväntat: `Finished release ... in Ns`. Inga warnings om `anthropic_api_key`.

- [ ] **Step 9.3: Manuell verifiering med dev-app**

```bash
cd "C:/Users/marcu/Documents/Programmering hemma/Temp/SVoice ver 3"
pnpm tauri dev
```

Manual test-plan:

1. **Pre-migration check:** Innan du startar appen, öppna `%APPDATA%/svoice-v3/settings.json`. Om den har `"anthropic_api_key":"sk-ant-..."` som klartext, notera nyckeln.
2. **Starta appen** och öppna Settings (tray → vänsterklick).
3. **Verifiera migration:** Om klartext-nyckel fanns, öppna `%APPDATA%/svoice-v3/settings.json` igen — fältet ska vara borta.
4. **Verifiera keyring:** Öppna Windows Credential Manager (Win+R → `control /name Microsoft.CredentialManager`). Under "Generic Credentials", sök `svoice-v3`. Entry med username `anthropic_api_key` ska synas.
5. **Verifiera UX:** Settings-fältet för Anthropic API-nyckel ska visa placeholder `••••••••`, inte värdet. Rensa-knappen ska synas under fältet.
6. **Test av action-popup (Claude):** Sätt provider → Claude. Tryck Insert, kör kommando. Popup öppnas, Claude svarar → paste fungerar.
7. **Test av Rensa:** Tryck "Rensa nyckel" → Spara. Verifiera att:
   - Credential Manager-entry försvinner
   - Action-popup visar "Ingen LLM-nyckel konfigurerad" vid nästa Insert
8. **Test av Sätt ny:** Skriv in nyckel, Spara. Verifiera att Credential Manager-entry återkommer och action-popup fungerar igen.

- [ ] **Step 9.4: Merge till main + tagga**

Om all manual-verifiering grön:

```bash
cd "C:/Users/marcu/Documents/Programmering hemma/Temp/SVoice ver 3"
git checkout main
git merge --no-ff iter4.5a/keyring -m "merge: iter 4.5a — Keyring för API-nycklar"
git tag iter4.5a-complete
git push origin main --tags
```

- [ ] **Step 9.5: Uppdatera `docs/superpowers/START-NEXT-SESSION.md`**

Lägg till `iter4.5a-complete` i tagg-listan + uppdatera *Vad som är kvar*-tabellen (stryk iter 4.5a-raden). Commit.

```bash
git add docs/superpowers/START-NEXT-SESSION.md
git commit -m "docs: uppdatera START-NEXT-SESSION efter iter 4.5a

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
git push origin main
```

---

## Exit-kriterier checklista

- [ ] `svoice-secrets`-crate med 5 gröna tester mot mock-backend
- [ ] `Settings.anthropic_api_key`-fältet borttaget från Rust + TS
- [ ] Migration körs vid app-start, verifierad end-to-end på dev-maskin
- [ ] Settings-UI visar `••••••••` + Rensa-knapp
- [ ] PTT (Claude) fungerar efter migration utan restart
- [ ] `set_anthropic_key` → hot-reload, action-popup använder nya nyckeln direkt
- [ ] `cargo test --workspace` grönt (≥ 27 tester)
- [ ] `cargo build --release` grönt, inga warnings
- [ ] Credential Manager-entry synlig under service `svoice-v3`, username `anthropic_api_key`
- [ ] Branch merged till `main`, tag `iter4.5a-complete` pushad

---

## Risk-noteringar

- **`keyring v3` mock-API kan ha blivit ändrat mellan minor-versioner.** Om `keyring::mock::default_credential_builder` inte finns: kolla `keyring`-cratens docs på docs.rs och använd motsvarande 3.x-API. Troligen behöver `#![feature(mock-keyring-rs)]` eller liknande feature-gate.
- **Tauri 2 invoke_handler kräver att nya commands syns både i `svoice-ipc`-export och i `generate_handler!`-makron.** Om frontend-anrop ger `command not found`: kontrollera båda ställen.
- **Mock-keyring är process-globalt.** Om tester körs parallellt kan de stomp på varandra. Kör `--test-threads=1` eller bygg per-test isolation via unika usernames.
